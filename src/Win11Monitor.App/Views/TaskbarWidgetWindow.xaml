<Window x:Class="Win11Monitor.App.Views.TaskbarWidgetWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Z690 Monitor"
        Width="430"
        Height="40"
        WindowStyle="None"
        ResizeMode="NoResize"
        AllowsTransparency="True"
        Background="Transparent"
        ShowInTaskbar="False"
        ShowActivated="False"
        Focusable="False"
        Topmost="True">
  <Border CornerRadius="5"
          Background="#F21A1D20"
          BorderBrush="#5A626A"
          BorderThickness="1"
          Padding="11,0">
    <Grid>
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="116" />
        <ColumnDefinition Width="1" />
        <ColumnDefinition Width="170" />
        <ColumnDefinition Width="1" />
        <ColumnDefinition Width="*" />
      </Grid.ColumnDefinitions>
      <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,11,0" ToolTip="CPU 最高 / 平均核心温度">
        <TextBlock Text="CPU" FontWeight="SemiBold" Foreground="#7FE0B7" VerticalAlignment="Center" Margin="0,0,8,0" />
        <TextBlock Text="{Binding CpuTemperatureCompactText}" FontWeight="SemiBold" FontSize="15" VerticalAlignment="Center" />
      </StackPanel>
      <Border Grid.Column="1" Background="{StaticResource BorderBrush}" Margin="0,8" />
      <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center" Margin="11,0" ToolTip="CPU 最高 / 平均核心电压">
        <TextBlock Text="{Binding VoltageSourceShortText}" FontWeight="SemiBold" Foreground="#EFC368" VerticalAlignment="Center" Margin="0,0,8,0" />
        <TextBlock Text="{Binding CpuVoltageCompactText}" FontWeight="SemiBold" FontSize="14" VerticalAlignment="Center" />
      </StackPanel>
      <Border Grid.Column="3" Background="{StaticResource BorderBrush}" Margin="0,8" />
      <StackPanel Grid.Column="4" Orientation="Horizontal" VerticalAlignment="Center" Margin="11,0,0,0" ToolTip="PCH 温度">
        <TextBlock Text="PCH" FontWeight="SemiBold" Foreground="#7FB7E8" VerticalAlignment="Center" Margin="0,0,8,0" />
        <TextBlock Text="{Binding PchTemperatureCompactText}" FontWeight="SemiBold" FontSize="14" VerticalAlignment="Center" />
      </StackPanel>
    </Grid>
  </Border>
</Window>
